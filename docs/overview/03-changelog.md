# 3. Change Log

## v2.0 — thay đổi so với bản gốc
| # | Thay đổi | Ảnh hưởng |
|---|---|---|
| 1 | Thanh toán online dùng **SePay thật** (VietQR/Open Banking), xác nhận qua **Webhook** thay cho "sandbox payment" mô phỏng. | payment-service, order flow, checkout saga, notification |
| 2 | Cập nhật 3 điểm quan trọng: admin scope (có account user), auth đầy đủ, thuật ngữ payment → SePay. | business, product |
| 3 | **Admin self-protection**: không tự khóa/xóa/hạ quyền; bảo vệ admin cuối cùng. | auth-service |
| 4 | Chuẩn hóa **API versioning** `/api/v1/...`, `/internal/v1/...`. | toàn bộ API, gateway, frontend |
| 5 | Bổ sung **Order State Machine** chặn admin cập nhật trạng thái không hợp lệ. | order-service |
| 6 | Định nghĩa **Checkout orchestration + Saga**; saga cơ bản đưa vào Sprint 3. | order-service, roadmap |
| 7 | Làm rõ **ranh giới auth-service ↔ user-service** (Identity vs Profile) + lazy-init. | auth-service, user-service |
| 8 | Hoàn thiện catalog: category conflict guard, Cloudinary image metadata/upload, variant wiring và product-first public listing không dùng dữ liệu giả. | product-service, frontend |
| 9 | Add Product chuyển sang guided flow; product mới mặc định INACTIVE, chỉ ACTIVE khi có variant và chặn xóa final variant của ACTIVE product. | product-service, frontend, docs |
| 10 | Admin Product Management hiển thị lỗi thân thiện, field guidance và recovery action; recoverable errors giữ nguyên drawer/form, image upload failure là partial success. | frontend, docs |
| 11 | Customer Shop category filter hiển thị **toàn bộ category thật** (gồm cả con) từ `GET /api/v1/categories`; endpoint public không param nay trả danh sách phẳng thay vì chỉ root. "Tất cả" là option FE để clear filter; không hardcode category. | product-service, frontend, docs |
| 12 | Admin Product Management: friendly error message dịch sang tiếng Việt; lỗi variant (add/update/delete, `LAST_ACTIVE_VARIANT`, duplicate SKU) hiển thị inline trong khu vực Variants của Edit Product drawer thay vì chỉ hiện toast; xóa variant/product/category dùng dialog xác nhận theo style dự án thay cho `window.confirm`/`window.alert`. | frontend, docs |
| 13 | Admin Category Management: Gateway dedupe thêm `Access-Control-Expose-Headers`/`Vary` (cùng nhóm với `Access-Control-Allow-Origin/Credentials` đã dedupe trước đó) để tránh CORS response header bị trùng lặp giữa Gateway và product-service; create/update/delete category có thông báo lỗi thân thiện tiếng Việt riêng khi không phải lỗi nghiệp vụ đã biết (`CATEGORY_IN_USE`/`CATEGORY_HAS_CHILDREN`/`SLUG_EXISTS`). | api-gateway, frontend, docs |
| 14 | Chuẩn hóa xác nhận/lỗi trên toàn bộ Admin Dashboard: thêm `AdminConfirmDialog` dùng chung, thay `window.confirm` cuối cùng còn lại (xóa tài khoản User Management) và bổ sung xác nhận cho vô hiệu hóa tài khoản, đổi trạng thái đơn hàng, gửi lại thông báo, tạo AI index job; lỗi self-protection/last-admin/invalid-transition/retry hiển thị inline cạnh action/row thay vì chỉ toast; thêm `admin-error-messages.js` chuẩn hóa 401/403/network sang tiếng Việt cho các trang chưa có helper riêng. | frontend, docs |
| 15 | Fix product image upload trả 500 chung chung khi Cloudinary chưa có credential: `ProductService.uploadImage` kiểm tra `ProductImageStorage.isConfigured()` trước, trả `IMAGE_STORAGE_NOT_CONFIGURED` (503) rõ ràng thay vì để exception rơi vào `IMAGE_UPLOAD_FAILED` (500); log lỗi upload thật sự nay gồm filename/content-type/size/loại exception (không log secret). Frontend có message riêng cho `IMAGE_STORAGE_NOT_CONFIGURED` trong khu vực Images. | product-service, frontend, docs |
| 16 | product-service tự nạp `BE/.env` khi chạy local từ IntelliJ (profile `local`), vì IntelliJ không tự source `.env` như `docker compose`. Ban đầu dùng `LocalDotenvEnvironmentPostProcessor` tự viết; sau đổi sang cơ chế chuẩn của Spring Boot `spring.config.import: optional:file:../.env[.properties]` trong `application-local.yml` — đơn giản hơn, không cần class Java riêng. Biến thật (OS env/system property/docker-compose) luôn có priority cao hơn, không bao giờ bị ghi đè; thiếu `.env` vẫn khởi động bình thường. Log chỉ ghi presence boolean + folder, không ghi giá trị secret. | product-service, docs |
| 17 | Fix cart hiển thị "Product"/"$0"/"One Size · Default" sau khi add-to-cart: `CartService.buildCartResponse` trước đây không bao giờ gọi lại product-service để lấy `variant`, nên `CartItemResponse.variant` luôn `null` và subtotal luôn `0`. Nay mỗi item được enrich qua `GET /internal/v1/products/variants/{variantId}` (tên/size/color/material/ảnh/giá hiệu lực); item không còn tồn tại hoặc không `ACTIVE` được đánh dấu `available: false` + `unavailableMessage` thay vì hiện giá giả; `totalAmount` tính đúng `unitPrice × quantity` (trước đây cộng dồn unitPrice mà bỏ sót quantity). Frontend tách `cart.mapper.js` khỏi `cart.api.js` để test được, hiển thị material khi có và trạng thái "không còn khả dụng" cho item mất. Checkout/order-service không đổi — vẫn tự lấy giá authoritative từ product-service khi tạo order. | cart-service, frontend, docs |
| 18 | Fix guest cart merge sau login mất guest session id khi merge thất bại: `cart.store.js`'s `mergeGuestCart` trước đây xóa guest session id trong `finally` bất kể thành công/thất bại; nay chỉ xóa khi merge thành công, còn thất bại thì giữ nguyên (để lần login sau vẫn thử gộp lại) và hiển thị thông báo tiếng Việt thân thiện thay vì mất dữ liệu âm thầm. Cơ chế merge (endpoint, cộng dồn quantity cùng variant, tách dòng riêng cho variant khác) đã đúng từ trước; xác nhận qua test controller/service mới cho tình huống merge khác variant và merge không có JWT. | frontend, cart-service, docs |
| 19 | Quản lý biến thể theo tổ hợp size/color/material/stock: `ProductVariant` thêm `stock_quantity`/`active` (migration `V3__product_variant_stock.sql` — bảng chưa có trường tồn kho nào trước đó, không có inventory-service riêng nên đây là schema change nhỏ nhất, hợp lý); `ProductService` chặn trùng tổ hợp size+color+material (so sánh không phân biệt hoa/thường, không kể material khác nhau) với `DUPLICATE_VARIANT` (409); admin variant form có "Số lượng tồn kho"/"Trạng thái" cùng validate `>= 0`. Root cause của bug hiển thị "trắng"/"Trắng" trùng lặp: `product.mapper.js`'s `uniqueValues()` dedup theo exact string, và `ProductDetailPage` hiển thị size/color như hai danh sách độc lập không cross-filter theo tổ hợp thật; `cart.store.js`'s `selectVariant` còn âm thầm fallback về variant đầu tiên khi không khớp tổ hợp, có thể thêm sai variant vào giỏ. Nay `product.variant-selection.js` (module thuần, có test) chuẩn hóa nhãn để gộp hiển thị nhưng vẫn khớp đúng giá trị gốc, cross-filter size/color theo tổ hợp thật, và trả về `null` (không đoán) khi tổ hợp không tồn tại hoặc hết hàng; Add to Cart chỉ bật khi có `variantId` hợp lệ và còn hàng. `cart-service.validateVariant` chặn thêm variant hết hàng/vô hiệu hóa (`VARIANT_OUT_OF_STOCK`). `ProductResponse`/`VariantSnapshotResponse` giữ nguyên các trường cũ, chỉ thêm `stockQuantity`/`active`. | product-service, cart-service, frontend, docs |
| 20 | Product Detail chuyển sang chọn variant theo flow size-trước: chọn size mới hiện màu thuộc size đó, size khác chỉ mờ đi (vẫn bấm được, không disable) để đổi thẳng sang size khác; bấm lại đúng size đang chọn thì bỏ chọn. Đổi/bỏ chọn size luôn tự xóa màu và `variantId` đã chọn trước đó (không giữ lựa chọn cũ của size cũ). Message Add to Cart tách theo từng bước thiếu: "Vui lòng chọn kích cỡ." (chưa chọn size), "Vui lòng chọn màu sắc." (đã chọn size, chưa chọn màu), "Biến thể này đã hết hàng." (đã chọn đủ nhưng hết hàng), thay cho một message gộp "Vui lòng chọn phân loại sản phẩm." dùng chung trước đây. | frontend, docs |
| 21 | Fix logout không xóa cart của user cũ: cơ chế merge guest cart sau login (mục 18) vốn đã đúng và có test đầy đủ ở backend; root cause thực sự là `auth.store.js`'s `logout()` trước đây chỉ xóa auth token/session, không đụng đến `cart.store.js` (`items`/`cartId` ở lại trong bộ nhớ) cũng như không xóa guest session id còn sót trong `localStorage` — nên cart/badge của user cũ có thể còn hiện sau logout, và guest session id cũ (nếu có) không được dọn. Nay `cart.store.js` có thêm `resetLocalCart()` (chỉ reset state cục bộ, không gọi API xóa cart trên server, để không xóa nhầm dữ liệu giỏ hàng thật của user); `logout()` gọi `resetLocalCart()` và `resetGuestSessionId()` cùng lúc với xóa auth session, để phiên guest tiếp theo luôn bắt đầu sạch. | frontend, docs |
| 22 | Localize toàn bộ Admin Dashboard UI sang tiếng Việt ở lớp hiển thị: sidebar, header, bộ lọc, badge, nút, tiêu đề trang, bảng, empty state và alert đều dùng copy tiếng Việt; các enum/payload values backend như `ACTIVE`, `PENDING`, `ADMIN`, `GOOGLE`, API path và DTO field giữ nguyên. | frontend, docs |
| 23 | Sync init-scripts với schema hiện tại: `auth_db` có thêm `audit_log` và index của nó để khớp Flyway V3; `order_db.orders.shipping_address` được siết `NOT NULL` để khớp entity/DTO validation. Fresh local DB initialization giờ bám đúng schema hiện tại của service. | backend, docs |
| 24 | Storefront product card cart icon không còn quick-add variant-mù nữa: bấm icon trên product card sẽ mở `/products/{productId}` để customer chọn variant, còn header cart icon vẫn đi `/cart`. Shop thêm filter target demographic `Tất cả / Nam / Nữ / Unisex` dựa trên field `targetDemographic`; backend public list hỗ trợ query param cùng tên, không cần schema mới. | frontend, product-service, docs |
| 25 | Customer profile page giờ là trang tài khoản thật: `/profile` (alias `/style-profile`) hiển thị thông tin đăng nhập, tách rõ tab style profile và địa chỉ giao hàng, form địa chỉ chỉ mở khi người dùng chủ động thêm/chỉnh sửa; checkout từ cart luôn khởi tạo session mới và không tái dùng state xác nhận/thanh toán cũ của order trước. | frontend, docs |
| 26 | Refactor domain Product: xóa `aestheticStyle`/`seasonalProperty` (season/style cấp product — khác style profile của user-service, không đụng tới); Product - Category chuyển many-to-one → many-to-many qua bảng mới `product_categories` (migration `V4__product_categories_and_english_demographic.sql`, backfill từ `products.category_id` cũ rồi drop cột); `targetDemographic` chuẩn hoá thành enum tiếng Anh `MALE`/`FEMALE`/`UNISEX` ở DB/API (dữ liệu seed cũ `MEN`/`WOMEN`/`UNISEX` và các alias Việt hoá được migration tự map, không giá trị nào rơi ngoài 3 enum sau khi chạy). `ProductRequest`/`ProductResponse` đổi sang `categoryIds`(request)/`categories: [{id,name}]`(response); admin form đổi single-select category thành chip đa chọn, bỏ 2 field "Phong cách thẩm mỹ"/"Mùa sử dụng"; Vietnamese `Nam/Nữ/Unisex` chỉ còn là label hiển thị (`product.demographic.js`), giá trị gửi API luôn là `MALE`/`FEMALE`/`UNISEX`. `init-scripts/03-product-db.sql` và file seed 1490-sản-phẩm được viết lại khớp schema mới (đồng thời vá luôn phần `product_variants.stock_quantity`/`active` bị thiếu từ trước so với Flyway V3). | product-service, ai-agent-service, frontend, init-scripts, docs |
| 27 | Customer Profile address management dùng modal xác nhận trong app cho xóa địa chỉ và bỏ thay đổi chưa lưu; không còn browser-native `alert()`/`confirm()` trong flow địa chỉ. | frontend, docs |
| 28 | Sync Docker init-scripts với current service schemas: `cart_items` giờ có `created_at`/`updated_at` để khớp `BaseEntity`, `notification_logs.id` chuyển sang `BIGSERIAL` để khớp entity `Long`, và product-service Flyway V4 backfill `product_categories` chỉ chạy khi legacy `products.category_id` còn tồn tại. Fresh Docker database now starts cleanly without schema-validation / Flyway failures. | backend, docs |
| 29 | Docker gateway routing now explicitly injects `NOTIFICATION_SERVICE_URL=http://notification-service:8089` into `api-gateway` so admin notification requests resolve inside the compose network instead of falling back to localhost. Product service adds migration `V5__product_images_updated_at.sql` and syncs `init-scripts/03-product-db.sql` so `product_images` includes `updated_at` to match `ProductImage extends BaseEntity`. | backend, docs |

Chi tiết kỹ thuật của mục 4–8 nằm trong thư mục `architecture/` và `decisions/`.
